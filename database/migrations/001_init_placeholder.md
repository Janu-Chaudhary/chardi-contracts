# Phase 1 schema (live in Neon)

The authoritative PostgreSQL schema for Trial Project A is already applied in Neon.

This repository keeps migration placeholders for documentation and future SQL exports.
Do not re-run destructive DDL against production without an explicit migration plan.

Confirmed tables:

- `scrape_runs`
- `scrape_errors`
- `opportunities`

See `docs/schema.md` for the application contract.
